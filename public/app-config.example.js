// Copy this file to app-config.js or rely on the GitHub Actions workflow to generate it.
// The values are safe to expose publicly (use the Supabase anon key, not the service role key).
window.__APP_CONFIG__ = {
  supabaseUrl: "https://your-project-ref.supabase.co",
  supabaseAnonKey: "your-anon-key"
};

